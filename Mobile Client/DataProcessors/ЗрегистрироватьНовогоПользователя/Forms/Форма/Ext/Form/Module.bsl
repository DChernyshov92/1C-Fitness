
&НаКлиенте
Процедура Зарегестрироваться(Команда)
	Если ЭтаФорма.Логин = "" 
		ИЛИ ЭтаФорма.Телефон = "" Тогда
		Сообщить("Для регистрации необходимо указать логин и номер телефона")
	КонецЕсли;
	
	АутентификацияИАвторизация.ВыполнитьРегистрацию(ЭтаФорма.Логин, ЭтаФорма.Телефон);
	ЗавершитьРаботуСистемы(Ложь, Истина);
КонецПроцедуры
